{
  "name": "todo-23",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently  \"cd backend-js && npm run dev\"  \"cd frontend && npm run dev\" \"cd backend && .\\Scripts\\activate && uvicorn main:app --reload\" "
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^8.2.1"
  }
}
